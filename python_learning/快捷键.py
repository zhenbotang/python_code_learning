"""
快捷方式：
shift + ctrl + alt + j :选中多个相同目标
alt + 鼠标点击 ：同时在多个位置输入相同内容
双击选中目标
ctrl + 模块名可以看模块代码
# 加快安装速度：pip install -i https://pypi.tuna.tsinghua.edu.cn/simple 包名称
# 按方向下键，即可跳转到本行最后

"""

"""
# 0 空列表定义
list90 = []
list91 = list()

# 空元组
t1 = ()
T2 = tuple()

# 定义空集合
a = set()  # 定义空序列的时候不管是什么类型，都用小括号?

# 空字典定义
dict1 = {}
dict2 = dict()
"""